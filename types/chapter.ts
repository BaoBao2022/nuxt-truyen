export interface Chapter {
    chapterId: string;
    chapterNumber: string;
    chapterTitle: string;
    updatedAt: string;
    view: string;
}

export interface Genre {
    genreTitle: string;
    slug: string;
}